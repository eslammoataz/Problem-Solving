vector<int>visited(n+1)  
for (int i = 0; i < n; ++i) {
        cin >> a[i];
        visited[a[i]]++;
    }
 
int mx=0 ;

for (int i = 0; i <= n; ++i) {
            if (visited[i] == 0) {
                mx = i;
                break;
            }
        }